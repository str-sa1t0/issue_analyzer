[project]
name = "issue-analysis"
version = "0.1.0"
description = "Utilities for fetching, analyzing, and commenting on GitLab issues."
requires-python = ">=3.14,<3.15"
dependencies = [
    "requests>=2.32.5,<3",
]

[tool.uv]
package = false
